name: Innoldb Test Coverage

on: 
  push:
    branches: 
      - master
      - feature/*
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ master ]

jobs:
  unit_tests:
    name: Unit Tests
    permissions: 
      repository-projects: write
      pull-requests: write
      checks: write
      id-token: write
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: actions/cache@v2
        id: cache
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install Dependencies
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          python -m pip install --upgrade pip
          pip install build
          pip install -r ./src/innoldb/tests/requirements.txt
      - name: Build test application
        run: |
          VERSION=$(cat version.txt)
          python -m build
          cd dist
          pip install innoldb-${VERSION}-py3-none-any.whl
          cd ..
        shell: bash
      - name: Run Unit Tests and Upload coverage
        run: |
          coverage run --source="./src/innoldb/" -m pytest --junit-xml=/tmp/cov/coverage-execution.xml
          coverage html --dir=/tmp/cov/
          cp .coverage /tmp/cov/.coverage
          git config user.email "gmoore@makpar.com"
          git config user.name "InnoLab bot"
          git remote set-url origin https://x-access-token:${GIT_TOKEN}@github.com/Makpar-Innovation-Laboratory/innoldb.git
          git add . 
          git stash
          git pull
          git checkout gh-pages
          mkdir coverage
          cp -a /tmp/cov/. ./coverage/
          git status
          ls
          ls coverage
          NOW=$(date)
          git add . && git commit -m "$NOW Automated Test Coverage"
          git push origin gh-pages
        shell: bash
        env:
          GIT_TOKEN:  ${{ secrets.GIT_TOKEN }}
